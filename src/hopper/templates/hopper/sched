{% for event in events %}
{{ event.id }}, {{ event.title }}, N, {{ event.start|date:"m/d/Y h:i A" }}, {{event.end|date:"m/d/Y h:i A"}}, {{event.track.title }}, , {{ event.desc }},{{ event.runners}}, , , , , , , {{event.resourceId.title}}
{% endfor %}
